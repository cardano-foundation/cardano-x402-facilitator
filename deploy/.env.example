# Copy to deploy/.env. The single PostgreSQL password defaults to "postgres"
# for local development. Set a strong value before any non-local deployment.
# Existing volumes retain their stored password; changing this setting does not rotate it.
# POSTGRES_ADMIN_PASSWORD=replace-with-a-strong-password

# Required for the light profile when using hosted Blockfrost.
BLOCKFROST_PROJECT_ID=
